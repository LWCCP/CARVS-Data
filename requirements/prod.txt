# Production-optimized dependencies for YouNiverse Dataset Enrichment Pipeline
# Minimal set for production deployment with performance optimizations

# Include base requirements
-r base.txt

# Production-specific optimizations
uvloop>=0.19.0; sys_platform != "win32"  # High-performance event loop
orjson>=3.9.0  # Fast JSON serialization
cython>=3.0.0  # Performance optimizations

# Production logging and monitoring
prometheus-client>=0.19.0
sentry-sdk>=1.38.0
newrelic>=9.2.0  # Optional APM
datadog>=0.48.0  # Optional monitoring

# Production database optimizations
psycopg2-binary>=2.9.9  # PostgreSQL adapter (if using PostgreSQL)
redis[hiredis]>=5.0.0  # Redis with C extension

# Production web server (if needed)
gunicorn>=21.2.0
uvicorn[standard]>=0.24.0

# Security enhancements
cryptography>=41.0.0
pyjwt>=2.8.0
passlib[bcrypt]>=1.7.4

# Production configuration management
kubernetes>=28.1.0  # If deploying to Kubernetes
docker>=6.1.0  # If using Docker

# Resource monitoring and optimization
pympler>=0.9  # Memory profiling in production
objgraph>=3.6.0  # Object reference tracking

# Production-grade HTTP client optimizations
httpx[http2]>=0.25.0  # HTTP/2 support
requests-toolbelt>=1.0.0  # Advanced request features

# Compression and serialization
lz4>=4.3.2  # Fast compression
zstandard>=0.22.0  # High-ratio compression
msgpack>=1.0.7  # Binary serialization

# Production utilities
supervisor>=4.2.5  # Process management
setproctitle>=1.3.3  # Process title management

# System resource management
resource>=0.2.1
signal-trap>=1.0.0

# Production error handling
rollbar>=0.16.3  # Error reporting service
bugsnag>=4.7.0  # Alternative error reporting

# Performance monitoring
py-spy>=0.3.14  # Production-safe profiler
pymongo[srv]>=4.6.0  # If using MongoDB

# Production networking
dnspython>=2.4.2  # DNS resolution optimization
pycurl>=7.45.2  # Alternative HTTP client

# Production file handling
watchdog>=3.0.0  # File system monitoring
fcntl-flock>=0.1.2; sys_platform != "win32"  # File locking

# Production caching
pylibmc>=1.6.3  # Memcached client
hiredis>=2.2.3  # Redis C client

# Production database connection pooling
sqlalchemy-pool>=1.3.0  # If using SQL databases
asyncpg>=0.29.0  # Async PostgreSQL (if needed)

# Production async optimizations
aiodns>=3.1.1  # Async DNS resolution
cchardet>=2.1.7  # Fast character detection

# Memory optimization
pymalloc>=1.0.0  # Memory allocator optimization (if available)